import {DomElement} from '../DomElement';
import {DOM} from '../../utils/DOM';

export class BoardsInfoPage implements DomElement{
    private 
    constructor(){
        
    }
    
    Render(renderTarget:DocumentFragment){
        var root = DOM.Create("div", {className:'BoardsInfoPage'});
    }
}